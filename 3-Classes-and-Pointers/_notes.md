# CLASSES:

A Python class is a blueprint or template for creating objects, 
which are instances of the class. It bundles data (attributes) 
and behavior (methods) into a single, reusable unit, enabling 
the principles of object-oriented programming (OOP)

# POINTERS

We can pointers in python by using dictionaries